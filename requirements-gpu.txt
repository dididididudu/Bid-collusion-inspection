# GPU 服务器部署依赖
# 安装: pip install -r requirements-gpu.txt

# === 核心依赖 ===
numpy>=1.24.0
Pillow>=10.0.0
jieba>=0.42.1
PyMuPDF>=1.23.0
scikit-learn>=1.3.0
imagehash>=4.3.1
datasketch>=1.6.0
networkx>=3.1
python-dateutil>=2.8.2

# === SBERT + GPU (关键) ===
# 先装 torch CUDA 版，再装 sentence-transformers
torch>=2.0.0  # 会自动匹配 CUDA
sentence-transformers>=2.2.0
transformers>=4.30.0

# === 可选 GPU 加速 ===
# optimum[onnxruntime-gpu]  # 如需 ONNX GPU 推理则取消注释
