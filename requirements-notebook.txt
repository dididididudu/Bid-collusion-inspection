# ============================================================
# Notebook 环境依赖（预装 PyTorch 2.3.1 + CUDA 12.1）
# ============================================================
# 使用说明:
#   pip install -r requirements-notebook.txt
#
# 环境特点（已预装）:
#   - ubuntu22.04-cuda12.1.0-py311-torch2.3.1-tf2.16.1
#   - PyTorch 2.3.1 + CUDA 12.1 ✅ 无需重复安装
#   - TensorFlow 2.16.1 ✅ 项目未使用但已存在
#   - Python 3.11 ✅
# ============================================================

# === PDF 解析 ===
PyMuPDF>=1.23.0

# === 中文分词 ===
jieba>=0.42.1

# === 机器学习 ===
scikit-learn>=1.3.0

# === 数值计算 ===
numpy>=1.24.0

# === 图片处理 ===
Pillow>=10.0.0
imagehash>=4.3.1

# === MinHash LSH ===
datasketch>=1.6.0

# === 图分析与日期处理 ===
networkx>=3.1
python-dateutil>=2.8.2

# === SBERT 语义匹配（复用已安装的 PyTorch）===
sentence-transformers>=2.2.0
transformers>=4.30.0

# === OCR 文字提取（二选一）===
# 方案一：PaddleOCR（中文识别效果更好，需额外安装 PaddlePaddle）
# paddlepaddle-gpu>=3.0.0  # CUDA 12.x 版本（见下方注释）
# paddleocr>=2.8.0
#
# 方案二：EasyOCR（基于 PyTorch，无需额外深度学习框架）
# easyocr>=1.7.0
#
# ⚠️ 注意:
#   PaddlePaddle GPU 版与 CUDA 版本需要匹配。
#   CUDA 12.1 推荐安装: pip install paddlepaddle-gpu==3.0.0rc0
#   如安装失败，脚本会自动降级到 EasyOCR。

# === 可选加速 ===
# optimum[onnxruntime-gpu]>=1.17.0  # ONNX Runtime GPU 加速（需手动安装）